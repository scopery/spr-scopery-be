package com.company.scopery.modules.elicitation.suggestion.domain.enums;

public enum SuggestionItemStatus {
    PENDING,
    APPROVED,
    EXECUTING,
    SUCCEEDED,
    FAILED,
    SKIPPED,
    REJECTED
}
